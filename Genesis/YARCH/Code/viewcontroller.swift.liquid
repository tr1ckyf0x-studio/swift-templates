//  Created by {{ developer_name }} on {{ current_date }}.

import SnapKit
import SharedRouter

protocol {{ submodule_name }}DisplayLogic: AnyObject { }

public final class {{ submodule_name }}ViewController: UIViewController, Navigates {
    // MARK: - Views

    lazy var contentView: Displays{{ submodule_name }}View = {{ submodule_name }}View()

    // MARK: - Properties

    let interactor: {{ submodule_name }}BusinessLogic

    private let router: {{ submodule_name }}Routes

    // MARK: - Lifecycle

    required init(
        interactor: {{ submodule_name }}BusinessLogic,
        router: {{ submodule_name }}Routes
    ) {
        self.interactor = interactor
        self.router = router
        super.init(nibName: nil, bundle: nil)
    }

    @available(*, unavailable)
    required init?(coder _: NSCoder) { fatalError("init(coder:) has not been implemented") }

    override public func loadView() {
        view = contentView
    }

    override public func viewDidLoad() {
        super.viewDidLoad()
    }
}

// MARK: - {{ submodule_name }}DisplayLogic

extension {{ submodule_name }}ViewController: {{ submodule_name }}DisplayLogic { }
