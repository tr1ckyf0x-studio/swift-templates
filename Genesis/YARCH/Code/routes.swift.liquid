//  Created by {{ developer_name }} on {{ current_date }}.

import SharedRouter

public protocol {{ submodule_name }}Routes {
    /// Маршрут для возврата на предыдущий экран
    func backOrDismiss(animated: Bool) -> SharedRouter.Route
}
